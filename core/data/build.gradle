apply from: "$rootProject.projectDir/android-lib.gradle"

android {
    namespace 'com.reza.data'
}

dependencies {
    // projects
    api project(":core:database")

    // places
    implementation libs.places
}